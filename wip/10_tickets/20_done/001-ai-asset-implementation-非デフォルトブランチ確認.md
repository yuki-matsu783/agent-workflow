---
type: ai-asset-implementation
status: todo
depends_on: []
---

# light-task-workflow に非デフォルトブランチ確認を追加

## 目的

`light-task-workflow` の手順1（状態確認）に、現在ブランチがデフォルトブランチと異なる場合の
確認フローを追加する（issue #9）。

## 完了条件（DoD）

- [x] `light-task-workflow/SKILL.md` 手順1に、現在ブランチとデフォルトブランチを取得する手順が明記されている
- [x] 両者が異なる場合に `AskUserQuestion` で「このまま現在のブランチで実施する / デフォルトブランチに切り替えて実施する / 中断する」を確認するフローが明記されている
- [x] デフォルトブランチへの切り替えが選ばれた場合、`git checkout` → `git pull` で最新化してから軽作業を継続する手順になっている
- [x] ヘッドレス実行（`claude -p`、CI）時は確認せず現在のブランチのまま進める旨が明記されている（`.claude/rules/claude-config-headless-awareness.md` 準拠）
- [x] 既存の手順1の前提（ファイル変更を伴う場合のみ実施）と矛盾しない

## 作業内容

1. `light-task-workflow/SKILL.md` 手順1のコマンド例に `gh repo view --json defaultBranchRef --jq '.defaultBranchRef.name'` を追加する
2. 分岐箇条書きに、現在ブランチ ≠ デフォルトブランチのときの `AskUserQuestion` フローを追加する
3. ヘッドレス実行時の挙動（確認せず現在のブランチのまま進める）を明記する
4. 差分を見直し、他の手順（手順2以降）との整合を確認する

## 作業ログ

### うまくいったこと

- `gh-feature/SKILL.md` 54〜76行目のデフォルトブランチ取得コマンド（`gh repo view --json defaultBranchRef --jq '.defaultBranchRef.name'`）をそのまま流用でき、新しいコマンド体系を考える必要がなかった
- 手順1の既存の分岐箇条書き（`wip/10_tickets/10_doing/` の有無、未コミット変更の有無）と同じ「条件 → 対応」の形式に揃えて追記したので、既存の読み手にとって違和感のない差分になった
- ヘッドレス時の方針は、同ファイル手順5（振り返り、118行目）が既に採っている「承認待ちで止まらず安全側の動作を続行する」という考え方をそのまま踏襲できた。新しい方針を発明せずに済んだ
- 既存の「`main` 上で変更する場合」の文言を「デフォルトブランチ上で」に一般化した。デフォルトブランチが `main` でないリポジトリでも矛盾しなくなった

### うまくいかなかったこと

- `.claude/docs/10_spec/ワークフロー入口ガード.md` への追記要否は事前調査（プラン段階）で判断できた（9行目に「入口スキルの中身は対象外」と明記されているため不要と判断）が、issue の受け入れ条件には「関連仕様書に影響があれば追記」としか書かれておらず、判断根拠を明示的に issue 側へフィードバックする手段が無かった。振り返りチケット・結果報告に記載することで代替する
