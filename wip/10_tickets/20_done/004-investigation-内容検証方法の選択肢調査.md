---
type: investigation
status: todo
depends_on: ["003-investigation-WF012実装とマージ状態検出手段の調査.md"]
---

# 内容検証方法の選択肢調査

## 目的

マージ進行中に限りWF012保護ファイルの編集を許可する際、「コンフリクトマーカー除去以外の改変を防ぐ」現実的な検証方法の選択肢と、それぞれの限界を洗い出す。

## 完了条件（DoD）

- [x] 調査観点「内容検証方法の選択肢」に対する答え（選択肢一覧と限界）が wip/20_plans/調査結果-conflict-wf012-exception.md の「調査サマリ」に書かれている
- [x] 根拠（ファイル・行・コマンド出力）が添えられている
- [x] 答えが出なかった問いは「リスク・未解決事項」に理由つきで残っている

## 作業内容

1. `merge-prep.sh`の既存の検証パターン（`git merge-tree --write-tree`の使い方、作業ツリーを変更しない設計）を確認し、参考にできる部分を洗い出す
2. 編集後の内容を検証する現実的な選択肢を列挙する（例: 有効なJSONであること、コンフリクトマーカー`<<<<<<<`/`=======`/`>>>>>>>`が残っていないこと、既存キーの削除が無いこと、`state`/`review_decision`等の特定キーの値が変化していないこと、等）
3. 各選択肢の実装コスト（フックで機械的に検証可能か）と限界（何を防げないか）を整理する
4. 全体計画の受け入れ条件「許可されるのはコンフリクトマーカーの除去に限られ、それ以外の内容改変を防ぐ」を満たす現実的な落とし所を提案する（結論はAIアセット設計フェーズで決めるため、ここでは選択肢と根拠の提示に留める）

## 作業ログ

### うまくいったこと

- merge-prep.shの既存の非破壊的検証パターン（git merge-tree）を参考に、5つの検証選択肢（A〜E）を実装コストと限界つきで整理できた

### うまくいかなかったこと

- git ls-files -u の実機検証はスキップ（investigationのBash許可リストに無く実行不可。書き込みを伴う検証も調査の範囲外のため）。設計フェーズでの検討事項として引き継いだ
